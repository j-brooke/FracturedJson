# fracjson CLI Change Log

## 1.0.1

Set `<InvariantGlobalization>true</InvariantGlobalization>` for greater compatibility with trimmed-down OS builds.

## 1.0.0 

Initial release of the new **fracjson** CLI app.